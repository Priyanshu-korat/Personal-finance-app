import { GoogleGenerativeAI } from '@google/generative-ai';

// You'd need a key to test, but wait, the API key is in process.env.GEMINI_API_KEY
// Wait, I can't run it locally if I don't have their API key locally!
